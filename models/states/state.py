class State:

    def __init__(self, code, age, state_name, next, once, chronic):
        self.code = code
        self.age = age
        self.state_name = state_name
        self.next = next
        self.once = once
        self.chronic = chronic
